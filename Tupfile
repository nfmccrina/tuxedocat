include_rules

GTESTDIR=googletest/src
TESTDIR=test_src

cpp_files=$(SRCDIR)/*.cpp $(TESTDIR)/*.cpp

: foreach $(cpp_files)  |> $(CXX) $(CXXFLAGS) $(INCLUDES) -c %f -o %o |> $(BUILDDIR)/%B.o

: $(BUILDDIR)/*.o $(GTESTDIR)/libgtest.a |> $(CXX) %f $(LDFLAGS) -pthread $(LIBS) -o %o |> $(BUILDDIR)/tuxedocat
