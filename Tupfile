include_rules

GTESTDIR=googletest/src
TESTDIR=test_src

cpp_files=$(SRCDIR)/*.cpp $(TESTDIR)/*.cpp

: foreach $(cpp_files)  |> $(CC) $(CXXFLAGS) $(INCLUDES) -c %f -o %o |> $(BUILDDIR)/%B.o

: $(BUILDDIR)/*.o $(GTESTDIR)/libgtest.a |> $(CC) %f $(LDFLAGS) -pthread $(LIBS) -o %o |> $(BUILDDIR)/tuxedocat
